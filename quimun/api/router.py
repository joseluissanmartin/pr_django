# from rest_framework.routers import SimpleRouter
# from quimun.api.views import ClienteApiViewSet, EmpresaApiViewSet, ArriendoApiViewSet

# router_quimuns = SimpleRouter()

# router_quimuns.register(prefix='clientes', basename='clientes', viewset=ClienteApiViewSet)
# router_quimuns.register(prefix='empresa', basename='empresa', viewset=EmpresaApiViewSet)
# router_quimuns.register(prefix='arriendo', basename='arriendo', viewset=ArriendoApiViewSet)
